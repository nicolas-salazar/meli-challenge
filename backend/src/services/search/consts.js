const { MELI_SERVICES_HOST } = require('../consts');

const CATEGORY_FILTER_ID = 'category';
const MELI_SEARCH_ENDPOINT = `${MELI_SERVICES_HOST}/sites/MLA/search`;

module.exports = {
  CATEGORY_FILTER_ID,
  MELI_SEARCH_ENDPOINT,
};
